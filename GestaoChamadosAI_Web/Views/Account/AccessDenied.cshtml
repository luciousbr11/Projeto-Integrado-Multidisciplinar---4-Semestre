@{
    ViewData["Title"] = "Acesso Negado";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 text-center">
            <div class="card shadow-lg border-0">
                <div class="card-body p-5">
                    <div class="mb-4">
                        <i class="bi bi-shield-exclamation text-danger" style="font-size: 5rem;"></i>
                    </div>
                    
                    <h1 class="display-4 text-danger fw-bold">Acesso Negado</h1>
                    
                    <p class="lead text-muted mt-3">
                        Você náo tem permissáo para acessar esta página.
                    </p>
                    
                    <hr class="my-4" />
                    
                    <p class="mb-4">
                        <i class="bi bi-info-circle text-info"></i>
                        Esta área é restrita a determinados tipos de usuários.
                    </p>
                    
                    <div class="d-grid gap-2 d-md-block">
                        <a asp-controller="Dashboard" asp-action="Index" class="btn btn-primary btn-lg me-md-2">
                            <i class="bi bi-house-fill"></i> Voltar ao Dashboard
                        </a>
                        <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn btn-outline-secondary btn-lg">
                                <i class="bi bi-box-arrow-right"></i> Fazer Logout
                            </button>
                        </form>
                    </div>
                </div>
            </div>
            
            <!-- Informações Adicionais -->
            <div class="mt-4 text-muted">
                <small>
                    Se você acredita que deveria ter acesso a esta página,<br />
                    entre em contato com o administrador do sistema.
                </small>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
}
