<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:GestaoChamadosAI_MAUI.ViewModels"
             xmlns:behaviors="clr-namespace:GestaoChamadosAI_MAUI.Behaviors"
             x:Class="GestaoChamadosAI_MAUI.Views.NovoChamadoPage"
             x:DataType="vm:NovoChamadoViewModel"
             Title="Novo Chamado">

    <Grid RowDefinitions="Auto,Auto,*">
        <!-- Logo + Botão Voltar -->
        <Grid Grid.Row="0" BackgroundColor="{StaticResource Primary}" Padding="15" ColumnDefinitions="Auto,*,Auto">
            <Image Grid.Column="0" Source="logo.png" WidthRequest="35" HeightRequest="35" HorizontalOptions="Start" VerticalOptions="Center"/>
            <Button Grid.Column="2"
                    Text="← Voltar"
                    Command="{Binding CancelCommand}"
                    HeightRequest="45"
                    WidthRequest="120"
                    FontSize="16"
                    BackgroundColor="{StaticResource Gray700}"
                    TextColor="White"
                    CornerRadius="8"
                    Padding="10,0"/>
        </Grid>

        <ScrollView Grid.Row="2">
            <VerticalStackLayout Padding="20" Spacing="20">

            <Label Text="Criar Novo Chamado"
                   Style="{StaticResource TitleLabel}"/>

            <Label Text="Preencha os dados abaixo. Nossa IA analisará automaticamente e sugerirá categoria e prioridade."
                   FontSize="14"
                   TextColor="{StaticResource Gray600}"/>

            <!-- Título -->
            <Label Text="Título *"
                   FontSize="16"
                   FontAttributes="Bold"
                   Margin="0,10,0,0"/>
            <Frame BackgroundColor="White" 
                   Padding="0" 
                   CornerRadius="8" 
                   BorderColor="{StaticResource Gray300}" 
                   HasShadow="False">
                <Entry Text="{Binding Titulo}"
                       Placeholder="Ex: Sistema não está abrindo"
                       PlaceholderColor="{StaticResource Gray400}"
                       TextColor="Black"
                       BackgroundColor="White"
                       ReturnType="Next"/>
            </Frame>

            <!-- Descrição -->
            <Label Text="Descrição *"
                   FontSize="16"
                   FontAttributes="Bold"
                   Margin="0,10,0,0"/>
            <Frame BackgroundColor="White" 
                   Padding="5" 
                   CornerRadius="8" 
                   BorderColor="{StaticResource Gray300}" 
                   HasShadow="False">
                <Editor Text="{Binding Descricao}"
                        Placeholder="Descreva o problema em detalhes..."
                        PlaceholderColor="{StaticResource Gray400}"
                        TextColor="Black"
                        BackgroundColor="White"
                        MinimumHeightRequest="150"
                        MaximumHeightRequest="300"/>
            </Frame>

            <!-- Mensagem de Erro -->
            <Label Text="{Binding ErrorMessage}"
                   TextColor="{StaticResource Error}"
                   FontSize="14"
                   IsVisible="{Binding ErrorMessage, Converter={StaticResource StringToBoolConverter}}"/>

            <!-- Botões - RESPONSIVO -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,20,0,0">
                <Grid.Behaviors>
                    <behaviors:ResponsiveGridBehavior Columns="2"/>
                </Grid.Behaviors>
                <Button Grid.Column="0"
                        Text="Cancelar"
                        Command="{Binding CancelCommand}"
                        BackgroundColor="{StaticResource Gray400}"/>

                <Button Grid.Column="1"
                        Text="Criar Chamado"
                        Command="{Binding CreateChamadoCommand}"
                        IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}"
                        BackgroundColor="{StaticResource Success}"/>
            </Grid>

            <!-- Loading -->
            <ActivityIndicator IsRunning="{Binding IsLoading}"
                             IsVisible="{Binding IsLoading}"
                             Color="{StaticResource Primary}"
                             HorizontalOptions="Center"
                             Margin="0,20,0,0"/>

            <!-- Dica -->
            <Frame BackgroundColor="{StaticResource Info}"
                   Padding="15"
                   CornerRadius="10"
                   Margin="0,20,0,0">
                <VerticalStackLayout Spacing="5">
                    <Label Text="💡 Dica"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="Quanto mais detalhes você fornecer, melhor será a análise da nossa IA e mais rápido será o atendimento!"
                           FontSize="14"
                           TextColor="White"/>
                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
    </Grid>

</ContentPage>
